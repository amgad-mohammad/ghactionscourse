name: First WorkFlow
on:
  push: 
      branches: 
        - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run : echo "Welcome to the our first github actions workflow"
      - name: Checkout Repository
        uses: actions/checkout@v7.0.1
      - name: List Repository files
        run: ls -la
      
